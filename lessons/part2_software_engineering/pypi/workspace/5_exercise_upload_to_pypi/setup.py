from setuptools import setup

setup(name='dsnd_mt_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['dsnd_mt_distributions'],
      zip_safe=False)
